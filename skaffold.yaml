apiVersion: skaffold/v2beta25
kind: Config
deploy:
  kubectl:
    manifests:
      - './infra/k8s/*'
build:
  local:
    push: false
  artifacts:
    - image: getlarge/ticketing-auth
      context: .
      sync:
        manual:
          - src: .
            dest: .
          # - src: 'apps/auth'
          #   dest: .
      custom:
        buildCommand: npx nx run auth:build-docker --conf=development --tag=latest --file=Dockerfile
        dependencies:
          command: node ./tools/nx/get-dependencies-cli.js -p auth
  tagPolicy:
    envTemplate:
      template: latest
